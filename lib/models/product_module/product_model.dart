class ProductModel {
  String? title;
  String? imageLink;
  double? oldPrice;
  double? newPrice;
  bool? isFavourite;

  ProductModel({this.title, this.imageLink, this.oldPrice, this.newPrice, this.isFavourite});
}
